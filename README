# Log Analytics Agent

## Overview
Log Analytics Agent is a lightweight, LLM-powered observability system that enables users to query Elasticsearch logs using natural language.  
It converts user queries into Elasticsearch DSL, executes them securely, and returns results in plain English.

The system is designed with a focus on determinism, security, and performance by combining rule-based templates with controlled LLM usage.

---

## Technologies Used

- **Backend:** Python  
- **LLM:** Llama3 (via Ollama)  
- **Search Engine:** Elasticsearch  
- **Pipeline:** ELK Stack (Filebeat, Logstash, Elasticsearch)  
- **HTTP Client:** Requests  

---

## Technology Rationale

| Component | Purpose |
|----------|--------|
| Python | Core language used to implement the query pipeline and orchestration logic |
| Ollama (Llama3) | Provides a locally hosted LLM for secure and offline natural language processing |
| Elasticsearch | Stores and retrieves log data efficiently using DSL-based queries |
| Intent Templates | Enables fast, deterministic execution for common queries without LLM dependency |
| DSL Validator | Ensures generated queries are safe, well-formed, and restricted from unsafe operations |
| Redactor Module | Masks sensitive data such as emails, IPs, and tokens before output processing |

---

## System Architecture

### Input Processing
- Accepts natural language queries including typos and abbreviations  
- Normalizes input using an LLM-based intent processor  

### Intent Resolution
- Matches query against predefined templates  
- If matched, generates DSL without LLM  
- Otherwise, forwards to DSL generation  

### DSL Generation
- Uses LLM with deterministic settings  
- Applies structured prompting  

### Validation Layer
- Blocks unsafe constructs  
- Enforces query size limits  

### Query Execution
- Executes DSL on Elasticsearch  
- Applies default time filter (last 24 hours)  

### Data Redaction
- Masks emails, IPs, tokens, and identifiers  

### Response Generation
- Converts results into plain English  
- Uses LLM only when required  

---

## Features

- Natural language querying  
- Deterministic outputs  
- Template-based optimization  
- Secure DSL validation  
- Sensitive data masking  
- Default time filtering  
- Fully local execution  
- Modular architecture  

---

## Setup and Installation

### 1. Clone the repository
```bash
git clone https://github.com/akrutipagar/log-analytics-agent.git
cd log-analytics-agent
```

### 2. Install dependencies
```bash
pip install -r requirements.txt
```

### 3. Configure environment
```bash
cp .env.example .env
```

### 4. Setup LLM
```bash
ollama pull llama3
ollama serve
```

### 5. Ensure Elasticsearch is running
- URL: http://localhost:9200  
- Index: application-logs-*  

### 6. Run application
```bash
python main.py
```

---

## Configuration

| Variable | Default | Description |
|---------|--------|------------|
| ES_URL | http://localhost:9200 | Elasticsearch endpoint |
| ES_INDEX | application-logs-* | Index pattern |
| OLLAMA_MODEL | llama3 | LLM model |
| DEFAULT_HOURS | 24 | Default time window |
| MAX_SIZE | 100 | Max documents returned |
| OLLAMA_TIMEOUT | 60 | LLM timeout |
| ES_TIMEOUT | 10 | ES timeout |

---

## Runtime Commands

| Command | Description |
|--------|------------|
| show dsl | Toggle DSL output |
| debug | Enable verbose logs |
| no time filter | Disable default time filter |
| exit | Quit application |

---

## Design Considerations

- **Deterministic Output:** Uses temperature = 0 for consistency  
- **Template-First Approach:** Reduces latency and avoids hallucination  
- **Secure Execution:** All DSL queries validated before execution  
- **Data Privacy:** Sensitive fields masked before output  
- **Modular Design:** Independent pipeline components  

---

## Future Enhancements

- REST API using FastAPI  
- Kibana dashboard integration  
- Advanced analytics  
- LangGraph orchestration  
- Unit testing  

---

## Author

Akruti Pagar  
B.Tech Instrumentation & Control — Cummins College of Engineering  
Diploma in Data Science & Programming — IIT Madras  
AWS Machine Learning Associate  
